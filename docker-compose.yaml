version: '3'
services:
  flask-app:
    build: .
    ports:
      - 5000:5000

  nginx-app:
    image: nginx
    ports:
      - 5001:5000
    depends_on:
      - flask-app
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
